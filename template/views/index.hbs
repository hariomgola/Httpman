<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>¯\_(ツ)_/¯ Hari HttpMan</title>
    <style>
      /* Global Reset */
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: 'Helvetica Neue', Arial, sans-serif;
        background: #f4f7fa; /* Light background for a professional look */
        color: #333;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        overflow: hidden;
      }

      .container {
        background-color: #fff;
        border-radius: 10px;
        box-shadow: 0 6px 18px rgba(0, 0, 0, 0.1);
        padding: 30px;
        width: 80%;
        max-width: 1000px;
        font-size: 16px;
      }

      h1 {
        text-align: center;
        color: #2a4d7c;
        font-size: 2rem;
        margin-bottom: 30px;
        font-weight: 600;
        letter-spacing: 1px;
      }

      /* Input Field Styles */
      .input-group {
        margin-bottom: 20px;
        display: flex;
        flex-direction: column;
        position: relative;
      }

      label {
        font-size: 14px;
        font-weight: 500;
        color: #555;
        margin-bottom: 8px;
      }

      select,
      input,
      textarea,
      button {
        padding: 14px;
        font-size: 16px;
        border: 2px solid #ddd;
        border-radius: 8px;
        background-color: #f9f9f9;
        color: #333;
        transition: all 0.3s ease;
        outline: none;
      }

      select,
      input,
      textarea {
        margin-bottom: 20px;
        width: 100%;
        box-sizing: border-box;
      }

      select:focus,
      input:focus,
      textarea:focus {
        border-color: #2a4d7c;
        box-shadow: 0 0 8px rgba(42, 77, 124, 0.3);
      }

      button {
        background-color: #2a4d7c;
        color: #fff;
        font-weight: 600;
        border: none;
        cursor: pointer;
        transition: background-color 0.3s ease, transform 0.2s ease-in-out;
      }

      button:hover {
        background-color: #1f3b62;
        transform: scale(1.05);
      }

      button:active {
        transform: scale(1);
      }

      button:focus {
        outline: none;
        border-color: #2a4d7c;
      }

      /* Textarea for JSON input & response */
      textarea {
        resize: vertical;
        min-height: 150px;
        background-color: #f4f7fa;
        color: #333;
      }

      #responseDisplay {
        background-color: #f9f9f9;
        color: #333;
        font-family: 'Courier New', monospace;
        padding: 15px;
        min-height: 180px;
        border-radius: 8px;
        border: 2px solid #ddd;
        word-wrap: break-word;
        overflow-y: auto;
      }

      .footer {
        text-align: center;
        margin-top: 25px;
        color: #888;
        font-size: 0.9rem;
      }

      /* Responsive Design */
      @media (max-width: 768px) {
        .container {
          padding: 20px;
        }

        h1 {
          font-size: 1.5rem;
        }

        select,
        input,
        textarea,
        button {
          font-size: 14px;
        }
      }

      /* Placeholder styling */
      ::placeholder {
        color: #aaa;
        font-style: italic;
      }

      /* Focus and Hover States */
      select:focus,
      input:focus,
      textarea:focus {
        outline: none;
        border-color: #2a4d7c;
      }

      select:hover,
      input:hover,
      textarea:hover {
        border-color: #1f3b62;
      }

      button:hover {
        background-color: #1f3b62;
        transform: scale(1.05);
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>¯\_(ツ)_/¯ Hari Httpman</h1>

      <div class="input-group">
        <label for="methodSelect">HTTP Method</label>
        <select id="methodSelect">
          {{#each methordAllowed}}
            <option value="{{this}}">{{this}}</option>
          {{/each}}
        </select>
      </div>

      <div class="input-group">
        <label for="urlInput">Enter URL</label>
        <input type="text" id="urlInput" placeholder="Enter your URL here..." />
      </div>

      <div class="input-group">
        <label for="jsonInput">Request Body (JSON)</label>
        <textarea id="jsonInput" placeholder="Paste your JSON here..."></textarea>
      </div>

      <div class="input-group">
        <button id="sendButton">Send</button>
      </div>

      <div class="input-group">
        <label for="responseDisplay">Response</label>
        <textarea id="responseDisplay" readonly>Your response will appear here...</textarea>
      </div>

      <div class="footer">
        <p>&copy; 2025 Hari Httpman</p>
      </div>
    </div>

    <script src="/js/app.js"></script>
  </body>
</html>
